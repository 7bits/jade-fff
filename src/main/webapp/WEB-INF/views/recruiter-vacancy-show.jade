extends layout

block append content
  .container
    h1 Просмотр вакансии
    dl.dl-horizontal
      dt Наименование
      dd #{vacancy.title}
      dt Описание
      dd #{vacancy.description}
      dt Диапазон з/п
      dd #{vacancy.salary}
      dt Дата создания
      dd #{vacancy.creationDate}
      dt Дата окончания
      dd #{vacancy.expirationDate}

    button.btn.btn-link(type="button")
      a(href=domainName + "/recruiter-active-vacancies") К списку вакансий
    a.btn.btn-primary(href="#") Предложить свою кандидатуру