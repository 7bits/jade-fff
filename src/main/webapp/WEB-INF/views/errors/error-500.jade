extends ../layout

block append head
  title=fmt.message("error-500.title", locale)
block append content
  .container
    .navbar-header
      a.navbar-brand(href=domain.buildFullUri("", locale)).
        #{fmt.message("error-500.h1", locale)}