extends ../layout

block append head
  title=fmt.message("error-403.title", locale)
block append content
  .container
    .navbar-header
      a.navbar-brand(href=domain.buildFullUri("", locale)).
        #{fmt.message("error-403.heading", locale)}